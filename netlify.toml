[build]
  command = "pnpm --filter @workspace/youtube-subs run build"
  publish = "artifacts/youtube-subs/dist/public"

[build.environment]
  NODE_ENV = "production"
  PORT = "3000"
  BASE_PATH = "/"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
